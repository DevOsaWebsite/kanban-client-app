import React from 'react';

import {EmptyBoard} from './_components/empty';

const BoardPage = (): React.JSX.Element => {
  console.log('BoardPage');

  return <EmptyBoard />;
};

// eslint-disable-next-line import/no-default-export
export default BoardPage;
